export enum MARKET_CAP_CATEGORY {
  MICRO = 'micro', // < 250 mil $
  SMALL = 'small', // >= 250 mil $ && < 2 bil $
  MID = 'mid', // >= 2 bil $ && < 10 bil $
  LARGE = 'large', // >= 10 bil $ && < 200 bil $
  MEGA = 'mega' // >= 200 bil $
}
